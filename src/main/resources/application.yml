spring:
  application:
    name: onepace

openai:
  api-key: ${OPENAI_API_KEY}
  model: ${OPENAI_MODEL:gpt-4o-mini}

app:
  frontend-url: ${FRONTEND_URL:http://localhost:5500,http://127.0.0.1:5500,https://www.onepace.site,https://onepace-1.vercel.app}
